<p id="notice"><%= notice %></p>

<h1>Paula and Jamie's Many Non-Linear Deals 🌻🐝</h1>

<div class="container">
  <div class="row">
    <% @deals.each do |deal| %>
      <div class="col-md-3">
        <%= link_to image_tag(deal.photo.url(:medium)), deal %>
      </div>
    <% end %>
  </div>
</div>

<br>

<%= link_to 'New Deal', new_deal_path %>
